---
"@mediawiki-typescript/api": minor
---

Add hand-authored precise response types and an `ActionApiClient` facade for a curated high-value Action API module set (`edit`, `query` info/revisions/categories/links/images/extracts/langlinks, `allpages`/`search`/`categorymembers`/`backlinks`, `delete`, `move`, `upload`, `parse`, `compare`, `opensearch`).
