---
"@mediawiki-typescript/api": minor
---

Wire `@mediawiki-typescript/api` into `mw-cli`, add opt-in end-to-end tests that exercise the Action and REST API clients against a live wiki, and finish package documentation.
